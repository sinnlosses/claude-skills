# 指示メモの履歴

## 2026-09-26

### エージェントのドラフト（承認あり）

ユーザーの言い回し（そのまま）:

> 開発フローやskillsをより汎用的にしたい。mainに依存していたり、特定のディレクトリ、ファイルに依存するものを洗い出し、設定できるように汎用性を出す提案をしてほしい。

> よろしく。あと、既存の適用しているリポジトリの移行方法の実装も含めてね

（エージェントのドラフト / 承認: 「よろしく。あと、既存の適用しているリポジトリの移行方法の実装も
含めてね」と、`/plan-tasks` の直前の「このまま /plan-tasks でタスクに割っちゃう？」への呼び出し）

対応表:

- 「主ブランチ `main` の固定をやめ、自動で決める」→ T-013
- 「`~/.claude` の決め打ちをやめる（install.sh・transcript.py）」→ T-014
- 「スキルの依存を宣言して install.sh が一緒に張る」→ T-015
- 「`init.py` の `check_direction` が `###` 見出しで数えるのをやめてしまう」→ T-016
  （登録中に踏んだ `taskfile.py` の禁止節の部分一致も同じタスクに入れた）
- 「difficulty の3段と実際のモデル名の対応表を正典に置く」（「汎用化しないと決めたもの」の中の1点）
  → T-017
- 「置き場の定義を1箇所に集める（設定にはしない）」→ T-018
- 「`docs/` の構造への依存を引数で開ける」→ T-019
- 「設定ファイルを `AGENTS.md` → `CLAUDE.md` の順で探す」→ T-020
- 「既存リポジトリの移行を実装する（コマンドにする。手順書で済ませない）」→ T-021（点検コマンド）と
  T-022（claude-skills 自身の片付け）
- 「入れる順番」→ タスクの `dependencies` に落とした（T-013 → T-018 → T-020 → T-021 → T-022、
  T-014 → T-015）

登録時に聞いて決めたこと（本文に焼き込み済み）:

- `AGENTS.md` 対応は**やる**。適用中の5リポジトリに `AGENTS.md` を置いているものは1つも無いが、
  読み取りを1関数に寄せるところまでは同じ作業なので今回に含める（T-020）
- `- 送り方:` は**今はやらない**。PR 運用のリポジトリが出てきたら考える
- 移行の適用は**claude-skills だけ**。他の4リポジトリは点検の出力を記録するところまで（T-022）

タスクにしなかったもの:

- 「`- 送り方:` を足して push と PR を選べるようにする」→ ユーザーが「送り方そのものを今はやらない」を
  選択。いまの「ローカルの ff マージまで、push しない」固定を残す
- 「tsukumo 側の追随タスクも要る」→ **事実と違った**。T-018 は定数の値を1文字も変えないので
  tsukumo の `task-summary.ts` に追随は要らない。ただし正典が書いている参照パス
  （`src/shared/task-summary.ts`）が実物（`src/server/repository/adapter/task-summary.ts`）と
  ズレていたので、そのズレの修正を T-017 に入れた
- 「移行の `--fix`（`- 送り方: ローカル` を1行足す）」→ `- 送り方:` を落としたことで既存リポジトリ側の
  書き換えがゼロになり、書くものが無くなった。T-021 は点検だけのコマンドにした
- 「difficulty の語彙を『軽/中/重』に変える」「設定の語彙の英語化」「`scripts/check_repo.py` の
  汎用化」「`python3` 直打ちの環境変数化」→ ドラフトの時点で「汎用化しないと決めたもの」。
  判断だけ残してタスクにしない

## 2026-09-20

### 会話から直接

ユーザーの言い回し（そのまま）:

> next-task をループしているときに direction.md になにか記載されるとループが止まるのを、止まらないようにしたいんだけど今ってどうなってるかな

> あー、なるほど。next-task はループを止めないように続くにしたいな

対応表:

- 「next-task はループを止めないように続くにしたいな」→ T-012

登録時に聞いて決めたこと（本文の `## 決まっていること` に焼き込み済み）:

- 3案（その場でタスク化して着手／タスク化だけして着手は次サイクル／報告だけ変えて `/loop`
  側に任せる）を出し、ユーザーは「その場でタスク化して着手」を選んだ
- 無人セッションで方針決めを伴うタスク化が走ることを承知のうえで許可する
- 他の停止シグナル（`doing` 残り・コンフリクト・`loopable: "N"` だけ・`INVALID`・
  `python3` の故障）は従来どおり止める

タスクにしなかったもの: なし

## 2026-09-16

### 追記（同日・会話から直接／5件目）

ユーザーの言い回し（そのまま）:

> 今のワークフローだとdirection.mdってキミも書けるんだっけ?

> なるほど。キミが書く場所の規則を決めておきたいね。direction.mdがドラフト、tasks.jsonが決まったタスク、で規則化するのはどうかな

> 方向性はいいね。ただ、ご主人様やアスナは固有の呼び名だからそこだけ別の表現にね

> いいよ

対応表:

- 「direction.md がドラフト、tasks.json が決まったタスク、で規則化」→ T-010
- 「固有の呼び名だからそこだけ別の表現にね」→ T-010（節名をユーザー／エージェントにする）と
  T-011（`skills/` 全体への約束として切り出し）

登録時に聞いて決めたこと（本文の `## 決まっていること` に焼き込み済み）:

- 節は `## ユーザーから` と `## エージェントのドラフト` の2つ
- エージェントのドラフトは承認を得たものだけタスク化し、`/loop` 中は触らない
- `/next-task` の「`/plan-tasks` が先」は `## ユーザーから` に中身があるときだけ

タスクにしなかったもの: なし


### 追記（同日・会話から直接／4件目）

ユーザーの言い回し（そのまま）:

> コミットが増えること自体に不都合はないんだけど、rebaseするの手間なら変えてもいいね

対応表:

- 「rebase するの手間なら変えてもいい」→ T-009

登録時に聞いて決めたこと（本文の `## 決まっていること` に焼き込み済み）:

- `--ff-only` + rebase をやめ、ふつうの `git merge` を既定にする
- マージコミットができた回だけ main で検証コマンドを通す（ff で済んだ回は通さない）

タスクにしなかったもの: なし


### 追記（同日・会話から直接／3件目）

ユーザーの言い回し（そのまま）:

> タスクに着手したらステータスを doing にしてほしいんだけど、今ってどうなってるかな

> 案Aでいいよ。あと、ブランチ運用だけどfeature/T-${タスクID}でブランチを切ってmainに
> マージするところまでをゴールとしようか

対応表:

- 「着手したら `doing` にしてほしい」→ T-007
- 「`feature/T-${タスクID}` を切って main にマージするところまでをゴールに」→ T-008

登録時に聞いて決めたこと（本文の `## 決まっていること` に焼き込み済み）:

- `doing` は案A（廃止せず、`/next-task` が着手時に書く）
- マージは fast-forward。マージ後に `feature/T-xxx` を削除する
- ブランチ運用は正典（`WORKFLOW.md`）の既定にする。`CLAUDE.md` の `- ブランチ:` 行で上書き可
- 作業中だった `tasks/docs-layout-and-loop-halt` は先に main へマージしてから新運用に移る
  （`ca1add2` まで ff マージ済み、ブランチ削除済み）

タスクにしなかったもの: なし


- 「1. `develop/` を廃して `docs/` に寄せるか」→ タスクなし（現状維持と決めたため）
- 「2. `direction.md` に中身があると `/next-task` が止まる仕様を直す」→ T-001
- 「3. `docs/` の共通ファイルを作る手順を分かりやすくする」→ T-002（置き場の固定と索引）、T-003（README の育て方の案内）

### 追記（同日・会話から直接／2件目）

ユーザーの言い回し（そのまま）:

> 事前に実行承認を得れば loopable を Y にできるものはそうしたいんだけど、タスク化するときに
> その手順を踏んでもらえるかな? できる限り N は避けたいんだ

決めたこと:

- 「元に戻せない／外部へ反映する」タスクは、承認の範囲を本文に書けば `Y` にしてよい
- 「対話的な検証が必要」だけは事前承認で `Y` にできない（受け入れに人が要るため）
- 会話から指示を拾う判定は「明示の指示のときだけ」。`/loop` 中は会話入力の経路を使わない

対応:

- `loopable` の定義の直しと、登録時に聞く手順 → T-006
- 上の決定を焼き込んで T-004・T-005 を `N` から `Y` に変更

---

### 追記（同日・会話から直接）

ユーザーの言い回し（そのまま）:

> もう1点、direction.md はユーザーが書くことを想定してて、キミなら tasks.json にそのまま
> 追加できると思ったけどどうかな? 1つステップが省けるよ

> ファイルの役割を決めよう。これで問題点は塞がる?
> - direction.md: ユーザーが入力する指示書
> - history/direction.md: ユーザーが入力する指示書の履歴
> - tasks.json: direction.md もしくは会話を経由してキミが起こしたタスク
> - history/tasks.md: タスクの履歴

> いいよ。ただ、既存のプロジェクトでは移行が必要だったりする?

対応:

- ファイルの役割の正典化・会話を入口に足す・履歴の改名 → T-004
- 利用側3プロジェクトの移行（履歴の改名 + `workflow.json` の廃止反映）→ T-005

---

スキルの運用について3点相談し、次のように決めた。

## 1. `develop/` を廃して `docs/` に寄せるか → 寄せない（変更なし）

`develop/`（毎セッション書き換わる作業状態）と `docs/`（意図したときだけ書き換える人向けの
成果物）は可変性と読者が違う。アーカイブが `develop/` → `docs/history/` の一方通行である
という形も、置き場が分かれていることで表現できている。現状維持。タスクは不要。

## 2. `direction.md` に中身があると `/next-task` が止まる仕様を直す

今は `skills/next-task/SKILL.md` の手順1で、`develop/direction.md` に見出し行以外の中身が
あればタスクを1件も実行せず終了し、`/loop` がそれを「続行不要」として受け取る。
これは非対称で損している。止めて得るのは「覆るかもしれないタスク1件を無駄に実行せずに済む」
ことだけなのに、失うのは「READY な todo が全部止まる」こと。しかも止めた先で `direction.md`
は自動タスク化されない（分解は方針決めを含むので `/next-task` は代行しない設計）ので、
止めても止めなくてもユーザーが戻るまで誰も何もしない。

直したい挙動:

1. `develop/direction.md` に中身があっても `/next-task` は止めない。完了報告に
   「未タスク化の指示あり（N行）」を1行添えるだけにする
2. 止めるのは `READY` が0件になったとき。そこで初めて「進めるものが無く、未タスク化の指示が
   ある → `/plan-tasks` が先」と報告して終了する
3. `/loop` が「続行不要」を受け取るのは 2 のときだけ

直す場所は `skills/next-task/SKILL.md` の手順1と完了報告のフォーマット、および
`skills/task-workflow/WORKFLOW.md` の「指示メモ（`develop/direction.md`）」節。
`skills/setup-tasks/SKILL.md` の `PENDING:` の扱いも、この方針と矛盾しないか見直す。

## 3. `docs/` の共通ファイル（architecture.md など）を作る手順を分かりやすくする

置き場の決め方が4通り混在しているのが原因。`docs/history/*` は規約で固定、`CONTEXT.md` と
`docs/adr/` は domain-modeling が固定、一方でアーキテクチャ提案は
`skills/architecture-proposal/SKILL.md:149` が「リポジトリの慣習に従う」、調査メモは
`skills/research/SKILL.md:12` が「慣例があればそこ、無ければ適切と思う場所」と、後ろ2つが
その場のモデルの判断になっている。さらに `docs/` に何が居るかの索引がどこにも無い。

新しいスキルは作らず、`task-workflow` が既にやっている「正典を1箇所に置く」形を真似する:

1. **置き場を規約で固定する。** アーキテクチャ提案は `docs/architecture.md`、調査メモは
   `docs/research/<topic>.md` に固定し、「慣習に従う」をやめる。`docs/history/` と同じ扱いにする
2. **`docs/README.md` を索引にする。** `docs/` に何かを書いたスキルは、そこに1行足すことだけを
   守る。このリポジトリの `README.md` の「由来の一覧が索引」と同じ発想で、ズレは検証コマンド
   （`scripts/check_repo.py`）で落とせるようにできないか検討する
3. **育て方を梯子として1箇所に書く。** 「新規プロジェクトでは何も作らない（遅延作成）→
   構造で迷ったら `/architecture-proposal` → 用語がブレ始めたら `CONTEXT.md` → 覆すのが
   高くつく決定をしたら ADR」。置き場は `README.md` の「プロジェクト側に要るもの」節の隣。
   各スキルはそこをスキル名で参照する（絶対パスを埋めない）
