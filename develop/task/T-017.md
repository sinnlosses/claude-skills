---
id: T-017
summary: 正典の記述と実物のズレ2件を直す（tsukumo の参照パス・difficulty とモデル名）
status: todo
difficulty: haiku
loopable: Y
dependencies: []
---

## 目的

`skills/task-workflow/WORKFLOW.md` に書かれている参照先が実物とズレているのを直し、
`difficulty` の3段と実際のモデル名の対応を1箇所にまとめて、ラインナップが変わったときに
直す場所が決まっている状態にする。

## 完了条件

- `WORKFLOW.md:105` の tsukumo 側の参照パスが実物に合っている
- `WORKFLOW.md` の「difficulty とモデルの切り替え」に「3段 → 実際に渡すモデル名」の対応表が1つあり、
  ラインナップが変わったらその表だけを直せばよいと読める
- `./check.sh` が通る

## 背景

- `WORKFLOW.md:105` は tsukumo 側の読み手を `tsukumo の src/shared/task-summary.ts など` と
  書いているが、実物は `src/server/repository/adapter/task-summary.ts`（`TASK_DIR_PATH = "develop/task/"`
  を持つ）。`src/shared/task-summary.ts` は存在しない
- 「difficulty とモデルの切り替え」の表は `haiku`・`sonnet`・`opus` の3語を基準と例つきで並べていて、
  **値がそのまま委譲先のモデル指定になる**と書いてある。台帳の語彙（`taskfile.py` の
  `difficulty-line`）もこの3語。実在のモデルには `fable` もあり、世代も上がるので、
  「3段」と「実際のモデル名」が同じ語であることに依存した記述が増えると追随できなくなる

## 決まっていること（蒸し返さない）

- **`difficulty` の語彙（`haiku`/`sonnet`/`opus`）は変えない。** 「軽/中/重」に変えると台帳・
  全スキル・tsukumo の表示まで動くため。対応表を足すだけにする
