---
id: T-022
summary: claude-skills 自身を config-doctor が 0 で通る状態にする
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-021]
---

## 目的

汎用化の最後に、このリポジトリ自身に残っている旧形式の残骸と、実態と合っていない CLAUDE.md の
記述を片付ける。あわせて他の4リポジトリの状態を点検して記録する（**直さない**）。

## 完了条件

- `task config-doctor` が claude-skills で終了コード 0 を返す
- `task status` の出力に `legacy_progress` の行が出ない
- `CLAUDE.md` の「## タスク運用」節の説明文が実態（`develop/task/` の1件1ファイル）に合っている
- `develop/progress.md` の「注意」4件のうち**いまも生きている2件**が、消えずに行き先へ移っている
  （どこへ移すかは正典「結果の書き方と知見の置き場」に従う）
- tsukumo・Git-Bulk-Maestro・gitlab-watari-dori・helm-yadokari で `config-doctor` を打った出力が、
  このタスクファイルの結果の節に貼られている
- `./check.sh` が通る

## 背景

- `CLAUDE.md` の「## タスク運用」節は本文で
  「`develop/tasks.json`・`develop/progress.md`・`develop/direction.md` で管理する」と書いているが、
  このリポジトリは**すでに新形式**（`develop/task/` に1件1ファイル）。3行の設定部分は正しい
- `develop/progress.md` が「移行の残り。8章の表で振り分けたら消す。」の1行つきで残っていて、
  `task status` が `legacy_progress	develop/progress.md	未解決 1 / 注意 4` を出している。
  中身は「未解決: なし」と「注意」4件:
  1. T-005 の成果を3リポジトリの main へ ff マージ済み（コミットSHA つき）— **履歴**
  2. 「タスクの成果は作業ブランチで止めず、ローカルの main へマージするまで自動でやってよい」
     （2026-09-16 にユーザーが指示。push は含まない）— **いまも生きている運用ルール**
  3. 3リポジトリの `docs/workflow.md` に `develop/workflow.json` の値の表が残っている。
     Git-Bulk-Maestro だけ削除済みで、helm-yadokari と tsukumo は未処理 — **いまも生きている未処理事項**
  4. ブランチ運用は T-008 で変わる、T-008 が done になるまでは `- ブランチ: 作業ブランチを切る` が
     有効 — **T-008 は済んでいるので無効**

## 決まっていること（蒸し返さない）

- **他の4リポジトリは直さない。** `config-doctor` の出力を記録するところまでで、適用はユーザーが
  別途決める（2026-09-26 に確認）
- 旧形式3リポジトリの `task migrate` はこのタスクの範囲外

## 注意

- 生きている2件（上の2と3）は**消さずに移す**。移し先が無いからと捨てない
- 2の内容は `- ブランチ:` の設定そのものではなく「どこまで自動でやってよいか」の承認範囲なので、
  設定の3行に書き足して解決しようとしない
